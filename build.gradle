plugins {
    id 'org.jetbrains.intellij' version '1.1.3'
}

configure(subprojects) {
    version = '1.18'
    project.ext.set('modVersion', version)
    group = 'unittest.gabor'

    apply plugin: 'org.jetbrains.intellij'

    repositories {
        mavenCentral()
    }
}
